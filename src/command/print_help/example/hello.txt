你还没有按照依赖，请安装一下，否则无法正常运行！！！
        npm config set registry http://registry.npm.taobao.org
        npm install --production
        npm install --omit=dev
    -[可选] 设置镜像源：（国内推荐）
    -[可选] 进入项目目录运行：（旧版 npm <= 7）
    *[必选] 进入项目目录运行：（新版）
O(∩_∩)O 你是个大佬啊, 但还是要安装一下, 原因如下：
    -没有安装依赖会运行出错，安装后重新运行本命令
    -npm install 安装全部依赖，执行耗时久（取决于网路）
    -npm install --omit=dev 只安装「dependencies」下依赖，执行耗时久（取决于网路）
    -镜像源国内推荐使用「npm config set registry http://registry.npm.taobao.org」
"Enjoy It!" 🎉 Thank you for using this project. q(≧▽≦q)
>
>
>
